@page "/authors"
@using BlazorServer.Model
@using Microsoft.AspNetCore.Authorization
@using BlazorServer.Components.Pages.AdminPanel.Buttons
@attribute [Authorize(Policy = "AdminOnly")]
@rendermode InteractiveServer
@inject IEntityService<Author> AuthorService
<div class="container mt-2">
    <div class="row">
        <div class="col-12 mb-3">
            <button class="btn btn-primary">Create Author</button>
        </div>
        <div class="col-12">
            <h3 class="mt-3">Authors</h3>
        </div>
    </div>
    @if (AuthorList.Count > 0)
    {
        <div class="row">
            @foreach (var author in AuthorList)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <div class="card-body">
                            <h5 class="card-title">@author.Name</h5>
                            <h6 class="card-subtitle mb-2 text-muted">ID: @author.AuthorID</h6>
                            <p class="card-text">
                                <strong>Nationality:</strong> @author.Nationality<br />
                                <strong>Bio:</strong> @author.Bio
                            </p>
                            <div class="d-flex justify-content-start">
                                <GenericEditButton TEntity="Author" Id="@author.AuthorID" EditUrlPattern="/edit-author/{0}" />
                                <span class="m-1"></span>
                                <GenericDeleteButton TEntity="Author" Id="@author.AuthorID" />
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-12">
                <label>No authors found.</label>
            </div>
        </div>
    }
</div>
@code {
    public List<Author> AuthorList { get; set; } = new();
    protected override async Task OnInitializedAsync()
    {
        AuthorList = await AuthorService.GetAll();
    }
}


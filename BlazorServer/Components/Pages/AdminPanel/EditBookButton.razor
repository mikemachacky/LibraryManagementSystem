@inject NavigationManager NavigationManager

<button class="btn btn-primary" @onclick="Edit"><i class="bi bi-pencil-square"/></button>

@code {
    [Parameter]
    public int Id { get; set; }
    void Edit()
    {
        NavigationManager.NavigateTo($"/edit-book/{Id}");
    }
}
